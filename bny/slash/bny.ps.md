Run `bny ps $ARGUMENTS` to show running bny processes.

Discovers processes via dispatcher pidfile, child pidfiles (orphan
detection), and pgrep fallback. Shows PID, elapsed time, and command.

Use `--json` for machine-readable Result envelope output.
