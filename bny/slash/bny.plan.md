Run `bny plan $ARGUMENTS` to create an implementation plan from template.

Guards: spec.md must exist (run `bny specify` first).

After running:
1. Fill in technical context, project structure, and complexity tracking
2. The plan is the input for `bny tasks`

Protocol: run `./dev/test` after any code change. See `bny/AGENTS.md`.
