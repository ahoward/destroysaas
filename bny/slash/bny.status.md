Run `bny status $ARGUMENTS` to show current feature state.

Shows which lifecycle phase the current feature is in:
specify → plan → tasks → implement

Use `--json` for machine-readable Result envelope output.
On the main branch, lists all features and their phases.
